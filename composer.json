{
	"name": "nextcloud/integration_notion",
	"description": "Notion integration in Nextcloud",
	"type": "project",
	"license": "APL-3.0-or-later",
	"autoload": {
		"psr-4": {
			"OCA\\Notion\\": "lib/"
		}
	},
	"minimum-stability": "stable",
	"require": {
		"php": "^8.1 || ^8.2 || ^8.3 || ^8.4"
	},
	"require-dev": {
		"nextcloud/ocp": "dev-master",
		"nextcloud/coding-standard": "^1.1",
		"phpunit/phpunit": "^10",
		"psalm/phar": "^6.8"
	},
	"scripts": {
		"lint": "find . -name \\*.php -not -path './vendor/*' -not -path './build/*' -print0 | xargs -0 -n1 php -l",
		"cs:check": "php-cs-fixer fix --dry-run --diff",
		"cs:fix": "php-cs-fixer fix",
		"test:unit": "vendor/bin/phpunit -c tests/phpunit.xml --colors=always --fail-on-warning --fail-on-risky",
		"psalm": "psalm.phar --no-cache",
		"psalm:fix": "psalm.phar --no-cache --alter --issues=MissingReturnType,ClassMustBeFinal --dry-run",
		"psalm:update-baseline": "psalm.phar --threads=1 --update-baseline",
		"psalm:update-baseline:force": "psalm.phar --threads=1 --update-baseline --set-baseline=tests/psalm-baseline.xml"
	},
	"config": {
		"platform": {
			"php": "8.1"
		}
	}
}
